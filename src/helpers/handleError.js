/**
 * handleError
 *
 * @param {*} bot
 * @param {string} channel
 * @param {*} error
 */
let Discord = require("discord.js")

module.exports = async (bot, channel, error) => {
console.log(channel)
}